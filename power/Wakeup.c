#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main() {

	char wakeup = (char *) malloc(sizeof(wakeup));
	int brightness = 100;
	int idleTime;

	switch(wakeup) {

		case idleTime = 3 || idleTime = 4:
			printf("Computer about to Sleep");
			brightness == 0;
		break;
		default idleTime = 0:
			printf("Computer not Asleep");
		break;
	}

}